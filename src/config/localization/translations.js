const en = require('./en.json');
const tr = require('./tr.json');
const ru = require('./ru.json');
const gr = require('./gr.json');

const translations = {
  en,
  tr,
  ru,
  gr,
};

module.exports = {
  translations,
};
